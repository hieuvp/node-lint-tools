const { run } = require('runjs');

/**
 * @param {String[]} args
 * @param {Object} opts
 * @param {boolean} opts.fix
 */
const eslint = (args, opts = {}) => {
  const { fix = false } = opts;
};

module.exports = {
  eslint
};
